import { db } from '@/lib/db'
export const dynamic = 'force-dynamic'
export default async function Page(){ const items=await db.learningPathItem.findMany({where:{isPublished:true},orderBy:{durationMin:'asc'},take:50}).catch(()=>[]); return <div><h1 className="text-3xl font-black">Learning path</h1><p className="mt-2 text-slate-600">Complaint aur documents process samajhne ke short lessons.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{items.map((i)=>{ const content=i.content as any; return <div key={i.id} className="rounded-3xl border bg-white p-5"><p className="text-xs font-bold uppercase text-emerald-700">{i.category} · {i.durationMin} min</p><h2 className="mt-2 text-xl font-black">{i.title}</h2><ul className="mt-3 list-disc pl-5 text-sm text-slate-700">{(content.lessons || []).map((l:string)=><li key={l}>{l}</li>)}</ul><p className="mt-3 rounded-2xl bg-slate-50 p-3 text-sm">Action: {content.action}</p></div>})}</div></div> }
