import { db } from '@/lib/db'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
export const dynamic = 'force-dynamic'
export default async function Page(){ const sources=await db.officialSource.findMany({ orderBy:[{status:'asc'},{category:'asc'}] }).catch(()=>[]); const needs=sources.filter((s)=>s.status!=='VERIFIED').length; return <div className="grid gap-6"><div><h1 className="text-3xl font-black">Official source verification</h1><p className="mt-2 text-slate-600">Official portals ko verify, review aur stale-link risk track karo.</p></div><div className="grid gap-4 md:grid-cols-3"><Card><CardHeader><CardTitle>Sources</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{sources.length}</p></CardContent></Card><Card><CardHeader><CardTitle>Needs review</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{needs}</p></CardContent></Card><Card><CardHeader><CardTitle>Verified</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{sources.length-needs}</p></CardContent></Card></div><div className="grid gap-4">{sources.map((s)=><Card key={s.id}><CardHeader><div className="flex flex-wrap items-center justify-between gap-2"><CardTitle className="text-base">{s.title}</CardTitle><Badge>{s.status}</Badge></div></CardHeader><CardContent><p className="text-sm text-slate-600">{s.category} · {s.state} · {s.department || 'N/A'}</p><p className="mt-1 text-sm text-slate-700">{s.verificationNotes || 'No note.'}</p><a className="mt-3 inline-flex text-sm font-bold text-primary" href={s.url} target="_blank" rel="noreferrer">Open →</a></CardContent></Card>)}</div></div> }
