import { db } from '@/lib/db'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
export const dynamic = 'force-dynamic'
export default async function Page(){ const [snapshots, users, complaints, paid] = await Promise.all([db.launchMetricSnapshot.findMany({ orderBy:{createdAt:'desc'}, take:20 }).catch(()=>[]), db.user.count().catch(()=>0), db.complaint.count().catch(()=>0), db.user.count({ where:{ plan:{ in:['PRO','FAMILY','AGENT'] } } }).catch(()=>0)]); return <div className="grid gap-6"><div><h1 className="text-3xl font-black">Metric snapshots</h1><p className="mt-2 text-slate-600">Growth, conversion and revenue signals for launch monitoring.</p></div><div className="grid gap-4 md:grid-cols-3"><Card><CardHeader><CardTitle>Users</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{users}</p></CardContent></Card><Card><CardHeader><CardTitle>Complaints</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{complaints}</p></CardContent></Card><Card><CardHeader><CardTitle>Paid users</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{paid}</p></CardContent></Card></div><Card><CardHeader><CardTitle>Saved snapshots</CardTitle></CardHeader><CardContent>{snapshots.length===0?<p className="text-slate-500">No saved snapshots yet.</p>:<div className="space-y-3">{snapshots.map((s)=><div key={s.id} className="rounded-xl border p-4"><b>{s.createdAt.toDateString()}</b><p className="text-sm text-slate-600">Users {s.activeUsers} · Complaints {s.complaintCount} · Conversion {s.conversionRate ?? 0}%</p><pre className="mt-2 rounded-xl bg-slate-50 p-3 text-xs">{JSON.stringify(s.snapshot, null, 2)}</pre></div>)}</div>}</CardContent></Card></div> }
