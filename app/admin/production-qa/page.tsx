import { db } from '@/lib/db'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
export const dynamic = 'force-dynamic'
export default async function Page(){ const runs=await db.productionQaRun.findMany({ orderBy:{createdAt:'desc'}, take:50 }).catch(()=>[]); return <div className="grid gap-6"><div><h1 className="text-3xl font-black">Production QA</h1><p className="mt-2 text-slate-600">Mobile, payment, storage, SEO aur launch readiness test records.</p></div><div className="grid gap-4 md:grid-cols-4">{['PASS','WARNING','FAIL','NOT_TESTED'].map((status)=><Card key={status}><CardHeader><CardTitle>{status}</CardTitle></CardHeader><CardContent><p className="text-3xl font-black">{runs.filter((r)=>r.status===status).length}</p></CardContent></Card>)}</div><div className="grid gap-4">{runs.map((r)=><Card key={r.id}><CardHeader><div className="flex flex-wrap items-center justify-between gap-2"><CardTitle className="text-base">{r.area}</CardTitle><Badge>{r.status}</Badge></div></CardHeader><CardContent><p className="text-sm text-slate-600">{r.device} · {r.browser} · {r.testedBy || 'Unassigned'}</p><pre className="mt-3 overflow-auto rounded-xl bg-slate-50 p-3 text-xs">{JSON.stringify({ checklist:r.checklist, issues:r.issues }, null, 2)}</pre></CardContent></Card>)}</div></div> }
