import { getLaunchChecks } from '@/lib/launch/checks'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
export default async function Page(){ const checks=await getLaunchChecks(); const passed=checks.filter((c)=>c.ok).length; return <div><h1 className="text-3xl font-black">Launch Checklist</h1><p className="mt-2 text-slate-600">Production jaane se pehle critical setup verify karo.</p><div className="mt-5 rounded-3xl border bg-white p-5 shadow-soft"><div className="text-4xl font-black">{passed}/{checks.length}</div><p className="text-sm text-slate-600">checks passing</p></div><div className="mt-6 grid gap-4">{checks.map((check)=><Card key={`${check.area}-${check.item}`}><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><CardTitle className="text-base">{check.item}</CardTitle><Badge>{check.area}</Badge></div></CardHeader><CardContent><p className={check.ok?'font-bold text-emerald-700':'font-bold text-red-700'}>{check.ok?'PASS':'NEEDS ACTION'}</p><p className="mt-1 text-sm text-slate-600">{check.note}</p></CardContent></Card>)}</div></div> }
